# Facility Cleaning Mobile

React Native + Expo mobile app scaffold for the cleaning management system.

## Run locally

1. Copy `.env.example` to `.env` and fill Supabase values.
2. Install dependencies:

   ```bash
   npm install
   ```

3. Start Expo:

   ```bash
   npx expo start
   ```
